import 'package:flutter/material.dart';

class CheckStudent extends StatelessWidget {
  const CheckStudent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
